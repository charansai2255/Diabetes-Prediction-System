# Diabetes Prediction System

## Setup

1. Copy your downloaded files:
   - diabetes_svm_model.pkl
   - scaler.pkl

   into this folder.

2. Install dependencies:

pip install -r requirements.txt

3. Run:

streamlit run app.py
